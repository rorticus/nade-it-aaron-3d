import {Engine, Scene} from "webgl-engine";

const background = require('../resources/images/lobby-background.png');

export class Lobby extends Scene {
    constructor(engine: Engine) {
        super();
    }
}

export default Lobby;